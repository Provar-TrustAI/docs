{/* Single source of the home-page "What's new" highlight. Update this ONE file each release
    (the /doc-land-release process does this). The link points at /changelog, which always shows
    the latest release at the top. */}

<Note>
  **Latest release · v2026.08.03.2** — Connect a live Salesforce Agentforce agent by authorizing once as an administrator, with no secret to hand over, and grade its runs on evidence captured while the turn happens. Check an agent's factual claims against the source material they should have come from with the new Groundedness judge. And see what a connected agent can actually do — its tools, their exact schemas, and its knowledge sources — in a versioned, reviewable Agent Profile. [See what's new →](/changelog)
</Note>
