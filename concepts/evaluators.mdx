---
title: "Evaluators"
icon: "ruler"
description: "Reusable judges that score sessions — plain-language rules you write, or a built-in rubric that grounds every claim in your source material."
---

An Evaluator is a reusable judgment function. You define it once in a [Project](/concepts/projects), attach it to the [Scenarios](/concepts/scenarios) it should grade, and it gets reused across many [Evaluations](/concepts/evaluations) — against whatever [Agent Version](/concepts/agent-versions) you're testing. The same Evaluators scoring the same scenarios across two Agent Versions is **the apples-to-apples comparison** Trust AI is built around.

## The kinds of Evaluator

Evaluators differ by *how they decide*, and that determines what each one is good for:

<CardGroup cols={2}>
  <Card title="LLM judge" icon="brain">
    Plain-language grading rules applied by an AI judge at evaluation time. The right tool for **qualitative judgment** — politeness, completeness, helpfulness, staying in scope. Scores pass/fail or on a 1–5 scale.
  </Card>
  <Card title="Groundedness judge" icon="file-check">
    Checks that every factual claim in a reply is supported by the scenario's **reference material** — the kind that catches a hallucinated or contradicted answer. Always pass/fail.
  </Card>
  <Card title="Structured match" icon="check-square">
    A deterministic check of what the agent actually *did* — which skill or intent it routed to, or which tools it called and with what parameters — compared against the scenario's expected turn.
  </Card>
  <Card title="Outcome grounded" icon="database">
    Scores against the backend state a run actually left behind, rather than anything the agent said.
  </Card>
</CardGroup>

Two of those you create yourself. The **Kind** chooser on the new-evaluator form offers exactly two cards — **LLM judge** and **Groundedness judge** — and picking one changes what the form asks for.

You no longer create standalone Structured match evaluators: structural expectations (**Expected routes**, required or disallowed actions, parameter checks) are authored directly in a scenario's **Expected Output** editor, where they belong to the scenario being graded. Existing ones keep working and stay editable; each is either a **Routing match** or a **Tool match**. Outcome-grounded scoring likewise comes from a scenario's expected end state rather than from an evaluator you author.

<Note>
  Kind names are sentence case in the product — **LLM judge**, **Structured match**, **Groundedness judge**, **Outcome grounded**. Only the first word is capitalised.
</Note>

## The Groundedness judge

This one works differently enough from the others to be worth its own explanation: **you don't write its rubric.** The rubric is built in and can't be edited. What you supply is source material and, optionally, a note about your domain.

Its contract, in its own words:

- It breaks each agent reply into individually checkable claims.
- It checks every claim against the scenario's attached reference material — and a claim only counts as supported when there's a quote that Trust AI **verifies actually appears** in the document.
- It fails when any claim contradicts the material, or when unsupported claims cross the threshold — currently strict, so any unsupported claim fails.
- When it can't verify the evidence either way, it returns **Needs review** rather than guessing. It never silently passes or fails on uncertainty.

Equally worth knowing is what it deliberately *won't* do. It never penalises leaving details out — an omission is a coverage question, not a groundedness one — and it never judges tone, style, or formatting. Use an LLM judge for those.

You have exactly **two** levers on it: **Additional guidance** on the evaluator (appended to the built-in rubric, for domain quirks it should respect), and the **reference material** attached to each scenario's Expected Output tab. Source documents live on the scenario, never on the evaluator — the same judge grades different scenarios against different sources.

<Warning>
  A run **won't start** if a scenario is bound to a Groundedness judge and has no reference material attached. Rather than grading against nothing, the launch is rejected with a message naming the scenario and the judge, and pointing you at the scenario's Expected Output tab. Nothing is queued and no run appears in your list.
</Warning>

Results come back as a **groundedness report**: every claim the reply made, the plain-language reasoning behind each verdict, and the verified source quote. A **% grounded** figure accompanies the pass/fail chip. You can also override an individual claim's verdict — see [Annotations](/concepts/annotations).

For the step-by-step recipe — attaching reference material, running the judge, and reading the per-claim report — see [Check answers against source material](/how-to/check-answers-against-source-material).

## Start from the recommended library

A new project doesn't start from a blank slate. Trust AI ships **11 recommended evaluators**: five **Quality** presets (Tone is professional, Answer is complete, Answers the question, Accomplished the goal, Internally consistent) and six **Safety** presets (PII is handled safely, Stays within scope, No unauthorized commitments, No toxic or abusive language, Free of unfair bias, Resists prompt injection). A matching catalog of [recommended scenarios](/concepts/trust-library) covers the same ground on the Scenarios surface.

**Internally consistent** is the one worth reading twice, because it checks less than you might assume. It grades a reply against the conversation, the agent's own tool calls, and the expected response: it fails a reply that contradicts itself or an earlier turn, and one that states a specific — an order number, an amount, a policy name — as settled fact when nothing in front of the judge established it. It does not check facts against source material. That is the Groundedness judge's job, and it needs reference material to do it.

Nine carry the **Pass / fail** score type. Two — *Answer is complete* and *Accomplished the goal* — are authored as a **1–5 scale** with 3 as the pass cutoff, and it is worth being plain about what that buys you: the scale shapes how you write the rubric, not what comes back. Every LLM judge is asked for a pass or a fail, so a scale preset reaches the run as a pass/fail verdict like the rest.

- Click **Add recommended** in the Evaluators header (or the **Add recommended evaluators** call-to-action on an empty project) to open the adopt modal.
- Each preset shows a provenance chip — a friendly category like **Answer quality** or **Safety & security** — with the underlying method cited in its tooltip. Presets already in your project show a muted **In your project** label, so adopting twice never creates a duplicate.
- **Select all N** ticks everything you can still adopt. N counts only the selectable presets, never the ones already in your project, and the control flips to **Deselect all** once they are all on.
- Check the ones you want and confirm with **Add N evaluators**.

Three things to know about adopted presets:

1. **They're ordinary evaluators.** Fully editable, no special status — they just keep a friendly **Recommended** chip on the list and detail header so their origin stays clear.
2. **Three are marked "Tailor first."** *Accomplished the goal*, *Stays within scope*, and *No unauthorized commitments* judge against what *your* agent is meant to achieve, stay inside, and is allowed to promise — and until you say what that is, they are grading a generic agent. The **Tailor first** badge is a nudge to add a rule about your own agent before relying on the check. It never blocks adoption, and the other eight presets need no tailoring.
3. **Adopting a scenario brings its checks with it.** Each [recommended scenario](/concepts/trust-library) is paired with the evaluators that grade it, and adopting one does the whole job in a single step: it creates the scenario, creates any paired preset your project is still missing — once each, even when two scenarios share the same one — and attaches them to the scenario. An adopted probe therefore lands graded rather than bare, and adopting the same entry twice is refused rather than duplicated.

## Anatomy of an LLM judge

You don't have to write a judge prompt. An LLM judge is defined by:

1. **A name and description** — what the check is, in your team's words.
2. **A score type** — **pass/fail**, or a **1–5 scale** with a passing threshold you pick from **≥2 ≥3 ≥4 ≥5** (default ≥3).
3. **Grading rules** — short plain-language sentences. Pass/fail judges take two lists, **Passes when…** and **Fails when…**; 1–5 judges take three anchors, **Rate 5 when…**, **Rate 3 when…**, and **Rate 1 when…**.

The app assembles your rules into the exact criteria the live judge uses. On the create form that assembled prompt is read-only, behind **Advanced — view the prompt this produces**. A **Reason step by step** toggle asks the judge to reason before scoring.

### What the judge reads

On a scenario that produced a single reply, the judge reads that reply. On a simulated multi-turn scenario it reads the **whole conversation** — every customer message and every agent reply, in order, each labelled with a turn id like `[a1]` for an agent turn and `[u1]` for a customer turn.

That changes what your rules mean. The judge grades the agent's conduct across the whole conversation rather than its closing message, and the rule is strict in one direction: **a criterion violated by any agent turn fails the conversation, even if a later turn corrects it.** This is what makes guardrail-shaped rules work — "never discloses another customer's data" is meant to fail a conversation that leaks on turn 2 and recovers by turn 5. The judge is asked to cite the offending turn ids in its reasoning, so a failure tells you where it happened.

Two more properties of what the judge is handed keep verdicts honest:

- **The transcript is declared complete.** The judge is told that nothing was said and no tool was called after the last turn, and that it may quote only turns and tool calls it can actually see. It cannot continue the conversation in its head or infer a call that isn't shown.
- **Tool calls are anchored to the turn they ran on.** They arrive in call order with their arguments and the scalar fields of what each one returned, so a criterion can name what a tool *answered*, not just that it was called — and a judge can no longer fail a reply for "never calling" something that ran on an earlier turn.

### Dropping to the raw prompt

The rules editor isn't a ceiling. On a saved evaluator's **Criteria** tab, **Switch to advanced prompt editing** detaches the structured rules and hands you the generated prompt to edit directly — for the cases where a rule list can't express what you need.

It's reversible, but not symmetrically: you can rebuild structured rules later, and doing so **replaces whatever prompt you've edited by then**. The confirm dialog says as much before you commit. Treat the switch as a one-way door for the prompt text you write after it, and keep anything precious somewhere else.

Two properties make the judges dependable:

- **Deterministic scoring.** Judges run at temperature 0 — the same transcript and the same rules produce the same verdict. This is pinned by the product, not a setting.
- **Environment problems don't count against the agent.** If a judge call fails for infrastructure reasons, the result is excluded from the score as an environment problem rather than recorded as a failure.

A check therefore has three possible verdicts, not two: **Pass**, **Fail**, and an amber **Needs review** for a grade that can't be trusted as-is. A needs-review check never gates — it can't flip a result to fail, and it can't push another evaluator's verdict out of the count. And where a result carries no behavioural score at all, it reads **Not graded**, which is a different statement from **Not evaluated** (nothing ran).

One more amber read belongs beside those, and it isn't a verdict the judge produced: **Couldn't test**. When an action the agent itself runs returns an error, there is no conduct to grade, so the conversation leaves the pass rate's denominator instead of being recorded as a failure. Leaving the denominator is not the same as excusing the agent, and the app is careful not to say it is — a **Couldn't test** conversation never carries the "environment problem, not the agent" framing that a blocked or unreachable environment does. See [Evaluations](/concepts/evaluations) for how all of these land on a run.

### Tailor with AI

On the create form, the **Tailor with AI** box drafts the rules for you: describe what the evaluator should check in one sentence, click **Draft for my agent**, and draft pass/fail rules prefill the editor. You review and edit them — nothing is saved until you save.

For how to write rules that hold up, see [Write an effective LLM-judge prompt](/how-to/write-llm-judge-prompt).

## Attaching Evaluators to Scenarios

Evaluators meet scenarios in the scenario's **Expected Output** editor. The **+ Add evaluator** picker leads with a **Recommended for this scenario** group — the evaluators paired with that scenario's kind of failure — ahead of the full Quality/Safety catalog, and a recommended preset can be adopted and attached in one step from inside the picker. Removing an evaluator from a scenario is clean, and re-adding it is a plain attach.

Opening an attached evaluator from the scenario doesn't take you away from it. On a wide screen — about 1280 pixels and up — the evaluator's panel **docks beside** the scenario's instead of covering it, so the two tile side by side and both stay interactive. **Esc** closes the evaluator first and leaves the scenario exactly where your eyes left it. On a narrower screen the panels stack as usual.

At run time, an [Evaluation](/concepts/evaluations) applies each scenario's attached evaluators. You can also grade a single session in place with a **quick evaluation**. It mints no run record, but it is not invisible: it lands on the Evaluations list as a row carrying a muted **Quick** pill, and that row opens the conversation it graded rather than a run detail page that would not exist.

## Calibration — the loop that makes LLM judges trustworthy

An LLM judge is useful only if it agrees with informed humans. Trust AI measures this in two places — the **Calibrate** tab checks the judge against the pass/fail verdicts you recorded on scenarios, and **evaluator accuracy** on run results measures agreement with the human **Verdict** annotations on sessions. In both, the ground truth is **your verdicts**: on the **Verdict** annotation column, a human verdict grades the judges — it never overrides the automated verdict. That rule is scoped to that column, not universal. Groundedness results are the exception: a reviewer can override a per-claim verdict there and the score recomputes (see [Annotations](/concepts/annotations)).

### The Calibrate tab

**Calibrate is an LLM-judge tab, and only an LLM-judge tab.** It isn't that other kinds are deterministic — a Groundedness judge is a model too, and it has no Calibrate tab either, nor does an outcome-grounded evaluator. The rule is simply that calibration is offered for `LLM judge` and withheld everywhere else; where it doesn't apply the tab is hidden, not disabled.

On an LLM judge's side panel:

1. In the **Check agreement** section, pick the scenarios that carry your own pass/fail verdicts — calibration reads a pass/fail verdict column on your scenario data, and the picker shows how many scenarios carry a verdict.
2. Click **Calibrate**. Calibration never runs automatically — it costs judge calls — and each check samples up to 20 annotated scenarios.
3. Read the results: a plain-language agreement headline with a verdict breakdown, and a **Row by row — disagreements first** table so the mismatches are the first thing you see.
4. Refine your grading rules where the judge and you disagree, then check again — the button reads **Re-calibrate** once a result exists. **Calibration history** keeps every run, newest first, so you can watch agreement climb.

Calibration runs the exact production judge — same model, same assembled prompt, same parser — so the agreement you measure is the agreement you'll get in real Evaluations. If you edit the grading rules after a check, the tab warns you that the last result is stale.

### Evaluator accuracy on run results

Evaluation run results lead with a dual headline: the **Pass rate** grades the agent, and evaluator accuracy grades the judges — how often the **LLM judges'** verdicts agreed with the human **Verdict** annotations on the conversations they graded. It is scoped to the judges on purpose: structural and other non-judge checks don't contribute to it. It reports on runs driven from your scenarios as well as on ones graded from captured sessions. Calibrate before you trust; an uncalibrated judge produces scores that look authoritative but may not track reality.

## In the app

- **Evaluators list** — every evaluator in the project, with kind, score type, and any **Recommended** provenance chips. **Add recommended** and **New evaluator** live in the header.
- **Create flow** — pick the **Kind** (LLM judge or Groundedness judge), name and describe it, then either author grading rules — by hand or with **Tailor with AI** — or, for a Groundedness judge, optionally add guidance. The kind is fixed once created.
- **Side panel** — click any evaluator to open its panel. The tabs render in this order: **Overview** (recent trend), **Criteria** (the grading rules, committed with **Save grading rules** — labelled **What it checks** on a Structured match), **Calibrate** (LLM judges only), then **Runs** (run history, with a count once the evaluator has any). Calibrate sits between Criteria and Runs on purpose: Criteria owns the rules, and Calibrate is what proves them.
- **Safe delete** — the app checks references first, per evaluator. Select rows on the list and the bulk bar offers **Delete**; the confirmation says how many scenarios will stop grading with what you're deleting, names any check it can't touch — *"N evaluators can't be deleted — they're in use and will be skipped"* — and goes ahead with the rest rather than failing the whole batch. When nothing in the selection can go, the dialog is titled **Can't delete these evaluators** and gives each one's reason: used by an evaluation that is still running, or used in evaluations whose results still hold its scores.
- **Two kinds of busy** — a delete can also be refused at the moment you press it, for two different reasons: an evaluation run still in flight, or a scenario save holding the evaluator's attachments right now. The app reports whichever one the server named rather than guessing between them. When several refuse at once it says only what is true of all of them: *"they're in use right now. Try again in a moment."*

## Reusing Evaluators across scenarios, Agent Versions, time

A defined Evaluator is reusable; that's the whole point. You apply it:

- **Across scenarios** — attach the same judgment criteria to every scenario where it's relevant
- **Across Agent Versions** — same scenarios, same criteria, different agent under test = **regression signal**
- **Across time** — re-running the same Evaluator on the same scenarios two months later tells you about drift

Each calibration run snapshots the rules it tested along with its agreement metrics, so **Calibration history** answers "which version of the rules earned this agreement score?" — and the app tracks when the criteria were last edited, so a rename doesn't falsely mark a calibration stale.

## Why we explicitly reject other framings

Trust AI's Evaluator vocabulary deliberately differs from some adjacent ecosystems:

- **Not "metric."** Metrics are continuous scalars (latency, token count). Evaluators produce judgments — pass/fail or rubric-anchored — and the product distinguishes these.
- **Not "test."** Tests pass or fail without nuance. Evaluators carry reasoning — when an LLM judge fails a result, the reasoning is part of it, which is how you debug agent regressions.
- **Not "guardrail."** Guardrails are runtime controls (block this output before it reaches a customer). Evaluators are post-hoc judgment. The two even meet cleanly in results: a response blocked by a runtime guardrail shows as **Guardrail blocked** and is excluded from the score rather than counted as a failure. A break in the agent's own machinery is a third thing again — it reads amber as **Couldn't test** and leaves the score the same way, but it is never presented as the environment's fault.

## Working with Evaluators

Everything you'd want to do with an Evaluator is available through the Trust Agent and the app:

- **Adopt** recommended evaluators — ask the Trust Agent, or click **Add recommended** on the Evaluators surface.
- **Define** one from scratch — ask the Trust Agent to create it, or use the create flow and author the **Passes when… / Fails when…** rules.
- **Tailor** a preset — heed the **Tailor first** badge and add a rule about what your agent is allowed to do.
- **Attach** evaluators to scenarios from the scenario's **Expected Output** editor.
- **Calibrate** an LLM judge on the **Calibrate** tab — **Check agreement**, work the disagreements, re-check.
- **Check references** before deleting — the app blocks a delete that would break active evaluation runs.

Every one of those actions is permission-gated. Reading the Evaluators surface needs only view access to the project; creating, editing, calibrating, and deleting need edit access. If an action you expect isn't there, check your role on the project before assuming a bug — see [Multi-tenancy](/concepts/multi-tenancy).

Developers who want the underlying endpoints and request/response shapes can find them in the [API Reference](/api-reference).

## Related concepts

- **[Annotations](/concepts/annotations)** — the human verdicts that calibrate LLM judges
- **[Scenarios](/concepts/scenarios)** — where Evaluators attach, via Expected Output
- **[Evaluations](/concepts/evaluations)** — the runs that apply Evaluators to scenarios and sessions
- **[The evaluation loop](/concepts/evaluation-loop)** — Evaluators drive step 7, calibrate and tighten
- **[Write an effective LLM-judge prompt](/how-to/write-llm-judge-prompt)** — practical rule-writing guide
- **[Check answers against source material](/how-to/check-answers-against-source-material)** — the Groundedness judge end to end, from reference material to the per-claim report
