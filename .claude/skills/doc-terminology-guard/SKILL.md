---
name: doc-terminology-guard
description: Repo-wide grep sweep that enforces the AGENTS.md vocabulary law with an explicit wire-truth allowlist — retired nouns (Scenario set, dataset rows), internal codenames (Paddington), banned icons (lucide bot), Source-chip vocabulary, Chat/Simulate, Expected routes / Routed To. Use before every landing, after every product rename, or when asked to "check terminology", "sweep for stale vocabulary", or "lint the docs' language".
user-invocable: true
---

# /doc-terminology-guard — the vocabulary law, mechanically enforced

`30942c1` (Datasets→Scenarios, 24 files) proved the method: grep every residual hit of the old
vocabulary and **classify each against an allowlist** instead of blind find-replace. That
discipline lived only in one commit message; this skill makes it a standing gate. `doc-validate`'s
style dimension is per-page and loop-bound — this is the cheap whole-repo sweep.

## The law (sync with `AGENTS.md` — that file is the authority, this skill is the detector)

| Pattern (case-insensitive unless noted) | Rule |
|---|---|
| `scenario set` | Retired noun (sets removed v2026.06.30.1). Allowed only in: `changelog.mdx` history entries, archive folders/tags, quotes of vestigial in-app strings explicitly framed as vestiges. |
| `dataset` | Wire-truth only: `dataset_id` wire keys in `api-reference/*.json` and changelog history. The `/v1/datasets` family was removed at v2026.07.08.1 — never describe it as a surviving legacy container, never as the surface/product noun. |
| `paddington` (any case) | Reader-visible prose: never. Allowed: wire-format keys and enum literals in `api-reference/*.json` (e.g. `emit_paddington_sessions`, `authored_via: paddington`) and `docs-plan/` internal material. The old Credits-breakdown leak is fixed (the row reads "Trust Agent conversations"). The live slash-command labels `/author-tdm-data-with-discovery` and `/create-dataset` are reportable product leaks, never allowlisted quotes. |
| `/paddington/`, `/v1/internal`, `/v1/tdm/` | Never anywhere published (including the OpenAPI files) — hard fail. |
| `tdm`, `Test Data Management` | Banned codename — hard fail in every published file including `changelog.mdx`. The product word is "test data" (plan, store, run report). Allowed only in `docs-plan/` and `.claude/`. |
| `Trust Library` | Not a shipped label. Never attributed to a button, tab, or modal; the shipped triggers are **Add recommended scenarios** / **Add recommended evaluators**. Allowed only as the docs' own page title/slug for the catalog page. |
| `ASI0\d`, `OWASP`, `RAGAS`, `G-Eval`, `promptfoo`, `LLM0\d` as a chip or category *label* | Never — the provenance chip renders a friendly category; codes live in its tooltip. Prose that names the standard ("OWASP Agentic Top 10 (2026)") is fine. |
| lucide `bot` icon (`icon: "bot"`, `icon="bot"`) | Never — Trust Agent surfaces use `sparkles`. |
| `uploaded` / `generated` / `simulated` / `synced` as a session *origin label* | Retired taxonomy. Source is exactly **Scenario / Playground / External**. (Verb uses — "sessions generated by the wizard" — are fine; judge the noun-label sense.) |
| `Ask mode`, `Ingest now`, `Add to dataset`, `New Run flow`, `Faithfulness judge`, `Environments & Connections`, project **Connection** tab (singular), `Default execution user`, `Permission test identities`, `Does not hallucinate`, `one-off evaluation` | Retired UI labels. Current: Chat/Simulate, Sync now, Evaluate, the Evaluation Launcher, Groundedness judge, the project **Connections** tab, **Default test user** / **Additional test users (permission sets)** (legacy form only) or **Run tests as different users (permission sets)** (the card), **Internally consistent**, quick evaluation. |
| `Workspace Settings → Connections`, workspace-scope `Connections` | The workspace admin tab is **Connected orgs**; **Connections** names the project-settings tab only. |
| `four denominator-excluded`, "environment problem" applied to all excluded outcomes | Five outcomes leave the denominator; only four are environment-attributed. `agent_error` reads amber **Couldn't test** (straight apostrophe in source). |
| `one-off` / `one_off` | Reader-facing name is **quick evaluation** with the **Quick** pill. |
| `confidence interval`, `not significant`, `p-value`, `standard error` | Banned statistics vocabulary on run-comparison surfaces; the verdicts are plain-English sentences. |
| `execution user`, `execution identity`, `Integration user` | Reconcile to **test user** vocabulary (see the retired-labels row). |
| A hardcoded model count, vendor list, model name, or default model beside the Trust Agent picker | Never — "the models your workspace offers". Deployments pin an allowlist. |
| A hardcoded built-in skill count (`11`, `15`, `16`, `20` beside "skills") | Never — the bundled set keeps moving. |
| `Revoke` beside standing approval | The shipped control is **turn off**. |
| `Routes` column on Sessions, `topics` | Sessions say **Routed To**; Scenarios say **Expected routes**. |
| `coming soon`, `roadmap` | Each hit must name something still unshipped at the current tag — verify against the release notes AND the flag roster in AGENTS.md (Automatic Full Run is default-off; shared-store test-data cleanup is dark). A stale "(coming soon)" for a shipped thing is a finding. |
| `row` for a scenario | A scenario is an entity with an SCN id, not a row. The only carve-out is quoting the Calibrate tab's own copy; the recommended-scenarios modal has no carve-out. |

## Procedure

1. Read the current `AGENTS.md` terminology + content-boundary blocks first — if this table and
   that file disagree, that file wins and THIS SKILL needs a PR.
2. Sweep published surfaces only: `*.mdx` at root + `concepts/ tutorials/ how-to/ snippets/` and
   `api-reference/*.json`. Exclude `docs-plan/`, `.claude/`, archives (`v20*/`), `README.md`,
   `CONTRIBUTING.md`.
   ```bash
   grep -rniE 'scenario set|paddington|\btdm\b|test data management|trust library|"bot"|ask mode|ingest now|add to dataset|one-off|coming soon' \
     --include='*.mdx' --include='*.json' . | grep -vE '^\./(docs-plan|\.claude|v20)'
   ```
   (Split per-pattern in practice; the combined grep is illustrative.)
3. **Classify every hit** — `violation` (fix it), `allowlisted` (say which rule), or
   `law-is-stale` (the product changed again → the fix belongs in `AGENTS.md` first, then here).
4. Output a classified table. In gate mode (invoked from a landing), any unclassified or
   `violation` hit blocks the landing; fix and re-run.

## When to run

- As a `/doc-land-release` gate step (before the worktrunk→main merge).
- Immediately after any release that renames product vocabulary (pair with `/doc-release-align` —
  its terminology findings update `AGENTS.md`, then this sweep finds the stragglers).
- Standalone whenever prose "smells like last release".
