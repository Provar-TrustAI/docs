{
  "name": "provar-demo-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Remotion compositor pipeline: raw Playwright recordings + timeline JSON \u2192 polished demo GIFs/MP4s",
  "scripts": {
    "preview": "npx remotion studio src/index.ts",
    "render": "npx remotion render src/index.ts DemoComposition",
    "render:gif": "echo 'GIF export needs ffmpeg on PATH. Render an mp4 first, then: ffmpeg -i out/<name>.mp4 -vf \"fps=15,scale=1200:-1\" -loop 0 out/<name>.gif'"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remotion": "^4.0.0",
    "@remotion/cli": "^4.0.0",
    "@remotion/renderer": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
