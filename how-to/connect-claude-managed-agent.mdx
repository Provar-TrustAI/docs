---
title: "Connect a Claude Managed Agent"
icon: "plug"
description: "Connect an Anthropic Managed Agent on Claude Platform on AWS to a Trust AI Project — in the chat with the Trust Agent, or from the connection form in Project Settings."
---

This how-to connects a **Claude Managed Agent** — an Anthropic Managed Agent running on Claude Platform on AWS — to a Trust AI [Project](/concepts/projects). When you're done:

- The agent is registered as an [Agent Version](/concepts/agent-versions) you can pick when you launch an Evaluation
- Trust AI can drive conversations against it — explorations, simulated scenarios, and scored evaluation runs — and each conversation lands in the Project as a [Session](/concepts/sessions)

Trust AI's role with this connector is deliberately narrow: it observes the agent and relays its responses, while the agent runs its own tools. You are not redeploying or reconfiguring the agent — you're pointing Trust AI at it.

There are **two ways to connect**, and they save the same connection:

- **In the chat** with the [Trust Agent](/concepts/trust-agent), through a secure connection card. This is the fast path and the one to reach for first.
- **From the connection form** in **Project Settings → Connections**. Use it when you're already in Settings, or when the chat isn't where you are.

<Note>
  Two different "agents" meet on this page. The **Claude Managed Agent** you're connecting is *your* agent — the one under test, tracked as an [Agent Version](/concepts/agent-versions). The **Trust Agent** is the in-app assistant that helps you do the connecting. They share a word and nothing else.
</Note>

## What you'll need

Whichever path you take, you type **three** values and **pick** two:

| Value | How it's entered | Where it comes from |
|---|---|---|
| **Anthropic API key** | Typed, write-only | A long-term Claude Platform on AWS key |
| **AWS Region** | Typed, e.g. `us-east-1` | Where your Claude Platform on AWS workspace runs |
| **Workspace ID** | Typed, e.g. `wrkspc_…` | The Anthropic workspace that owns the agent — find it at `platform.claude.com/settings/workspaces` |
| **Agent** | Picked from a list | Discovered for you once the three above are filled in |
| **Environment** | Picked from a list | Discovered alongside the agent |

You don't look the **Agent** and **Environment** up. Both surfaces gate them behind a **Find agents** button that reads your workspace and turns them into dropdowns of human names — the underlying identifiers are stored, never typed.

You'll also need a **Trust AI Project**. If you don't have one yet, start with [Your first 10 minutes in Trust AI](/tutorials/quickstart).

## Connect in the chat

<Steps>
  <Step title="Ask the Trust Agent">
    Open a chat in your Project and ask:

    > *"Connect my agent"* — or *"I want to connect my Claude-managed agent."*

    The Trust Agent first checks what's already connected, so it doesn't make you reconnect something that works. If nothing is connected — or you tell it you want a new one — it asks which platform your agent runs on. It never assumes.

    For a Claude-managed agent it does **not** send you to Settings. It opens a secure connection card directly in the conversation and waits.
  </Step>

  <Step title="Enter your key, region, and workspace">
    **You should see:** a card headed **Connect your Claude Managed Agent**, with a lock icon and the line *"Collecting your API key securely — it's encrypted, entered only here, and never shown or stored in this chat."*

    Fill in **Anthropic API key**, **AWS Region**, and **Workspace ID**. The key field is masked as you type.
  </Step>

  <Step title="Find your agents">
    Click **Find agents**. Until all three values are filled in the button is disabled and the card says *"Enter your API key, region, and workspace ID first, then find your agents."*

    **You should see:** two new dropdowns, **Agent** and **Environment**, listing what that key can reach by name. Pick one of each. Changing the key, region, or workspace afterwards clears the results, so you never save a pick that belonged to different credentials.
  </Step>

  <Step title="Connect">
    Click **Connect**. Trust AI saves the connection and immediately runs a real check against it — see [The check that runs on save](#the-check-that-runs-on-save).

    The Trust Agent then reports the result in plain language — **Connected to** followed by your agent's name when the check came back healthy, or *"Connection set up — the health check came back …"* naming the outcome — *unreachable*, for example — when it didn't. That word is the chat's own phrasing of the result, not the pill you'll see on the Connections card; see [What the connection card says](#what-the-connection-card-says). The reason behind an unhealthy result is recorded on the connection itself, not written into the chat. No key and no secret go back into the conversation.

    **Close setup** closes the card without connecting; the Trust Agent replies *"Connection setup closed. Nothing was connected. Ask me to connect an agent when you're ready."* A closed card leaves that line and nothing else — there is no button on it to bring it back. Ask the Trust Agent to connect again and it opens a fresh card.
  </Step>
</Steps>

### Your API key never reaches the model

This is the reason the card exists, and it is worth being precise about.

- The Trust Agent opens the card by calling a tool whose arguments carry only the platform and the project. There is no field on that call a secret could travel in.
- Your key is typed into the card and sent straight to Trust AI's backend. It is not part of the conversation, so the model that is helping you never sees it.
- What comes back to the conversation when you finish is the **outcome only** — whether the connection is live, the human names of the agent and environment you picked, and the health result. The saved connection's own id rides along as a routing key and is never rendered anywhere you can see it. No key, no secret.
- If you paste something that looks like an API key into the chat by mistake, Trust AI strips it before it is stored; the Trust Agent sees `[redacted API key]` and will ask you to use the card instead.

<Note>
  A saved key is write-only everywhere afterwards. It is never displayed back — not on the connection card, not in any form, not in API responses. To change it, you connect the agent again with a new one.
</Note>

## Connect from Project Settings

The connection form is the manual path. It is where you go when you'd rather fill in a form than talk to the Trust Agent, and it saves exactly the same connection.

<Steps>
  <Step title="Open the connection form">
    In your Project, open **Project Settings**, select the **Connections** tab, and click **Connect an agent**.

    **You should see:** a page headed **Connect an agent** with **four** platform cards under it — **AWS Bedrock AgentCore**, **Salesforce Agentforce**, **Claude Managed Agent**, and **Conversational HTTP agent**. Choose **Claude Managed Agent**.

    The form opens in place, still under the **Connect an agent** heading, with a **Claude Managed Agent** chip, a **Change** link beside it and the platform's one-line description underneath. There is no field to set the platform — you already chose it in the picker. The one way out of any step is **Back to connections** in the page header; the form shows no Cancel of its own.
  </Step>

  <Step title="Name the connection and fill in the three values">
    Give the connection a **Connection Name** you'll recognize later — it becomes the label of the Agent Version, so "Support agent (staging)" beats "Claude 1".

    Then fill in **Anthropic API key**, **AWS Region**, and **Workspace ID**.
  </Step>

  <Step title="Find your agents">
    Click **Find agents**, then pick from the **Agent** and **Environment** dropdowns. Before discovery runs they are disabled and read *"Find agents to choose"*.

    An **Enter IDs manually** link reverts both to plain text inputs. It exists for the rare case discovery can't reach a resource; the discovery path is the one to use.
  </Step>

  <Step title="Test the connection (optional)">
    **Test Connection** runs one authenticated probe against the agent and environment as entered, and reports success or the reason it failed above the buttons. Nothing is saved by the test.

    The test is optional — the check that actually matters runs on save.
  </Step>

  <Step title="Save the connection">
    Click **Save connection**. You land back on the **Connections** tab with the new connection on it — see [What the connection card says](#what-the-connection-card-says).
  </Step>
</Steps>

## The check that runs on save

Saving a Claude Managed Agent connection is not a silent write. Trust AI opens one short-lived, authenticated session against the agent and environment you picked, records what happened, and that result is what the connection's reachability pill reports.

The check records a reachable connection when the session opened and an unreachable one when it didn't — and an unreachable result carries the reason, for example *"Anthropic API key invalid or expired."*, so you can fix the right field instead of guessing.

A connection is saved either way. An unreachable result means the credentials didn't work, not that nothing was stored. There is no form to go back and correct it in, so the fix is to connect the agent again with the corrected value and remove the connection that didn't work.

## What the connection card says

On the **Connections** tab the saved connection is a card. It leads with the name you gave it and a **Claude Managed Agent** chip, and the sub-line names the Anthropic workspace it points at. No credential appears on the card — there is no values grid and no masked secret.

Two status reads sit to the right of the name, and they answer different questions:

- **Is setup finished?** A setup badge — **Setting up**, **Needs admin** or **Setup failed** — shown only while setup still has something to say. A connection that finished setting up carries no badge at all; the absence is the good news.
- **Can Trust AI reach it right now?** A reachability pill — **Connected**, **Partly reachable**, **Can’t reach it** or **Not checked yet**. A connection nobody has probed reads **Not checked yet**; health is never fabricated green.

<Note>
  Those are the Connections vocabulary, and they are not the only health words in the product. The Trust Agent's side rail describes its **Connected agent** with a different four — **Not checked**, **Healthy**, **Degraded**, **Unreachable** — and the chat's own connect receipt phrases the same result differently again. Three surfaces, three sets of words for a related question; read each in its own place.
</Note>

Under the name, **Customize environment** expands the card in place — where this connection's test data lives, how it resets between tests, and how many tests may run against it at once. See [Manage a project's connections](/how-to/manage-project-connections) for what the expander holds and what each status word means.

### Where the Agent Profile opens

There is no profile entry point on the Connections tab, for this platform or any other. A connected build's [Agent Profile](/concepts/agent-profile) opens from the **Playground** topbar: pick the build in the agent picker, then press the info button beside it, tooltipped **Agent profile**.

Capture for a Claude Managed Agent lists the agent's real tool catalog from the MCP servers its own configuration declares — a metadata read, not a conversation with the agent. An agent that declares no such server, or one whose server doesn't answer, comes back as a name-only profile with the tool-catalog gap stated in plain words; it is never a failed capture and never a fabricated empty catalog. [Review an Agent Profile](/how-to/review-an-agent-profile) walks the fly-in.

## Remove a connection

**Remove** on the card is the only connection-level action besides the expander. There is no **Edit**: once a Claude Managed Agent connection is saved, there is no door back into the form it was created in, and no way to change a stored value in place. To change the key, the region, the workspace, the agent or the environment, connect the agent again and remove the old connection.

**Remove** opens a confirmation dialog headed **Remove this agent connection?**. It names the connection and says the removal takes it and its test data setup out of **this project only** — connections to the same agent in other projects stay unchanged, nothing is removed from the service that hosts it, and you can connect it again later. It closes with *"You can’t undo this."* The buttons are **Cancel** and **Remove connection**. Removing also archives the agent versions the connection provided, so the Playground and the evaluation launcher stop offering an agent that could only fail — see [Manage a project's connections](/how-to/manage-project-connections).

## Where sessions come from

A Claude Managed Agent connection has no historical session import. The **Sync now** dropdown on the Sessions page lists every connected agent grouped by platform, but historical sync supports AWS Bedrock AgentCore only. Picking a Claude Managed Agent there returns a typed *"Sync not supported"* message naming the platform, rather than silently importing nothing.

Sessions for this connector are created when Trust AI drives the agent:

- **Explorations** the Trust Agent runs after connecting, so you can watch your agent working before anything is scored
- **Simulated conversations** — from the [Playground](/concepts/playground), or fanned out per persona when an evaluation runs [scenarios with simulation](/how-to/simulate-with-scenarios)
- **Evaluation runs** against the connected Agent Version

Each lands on the [Sessions](/concepts/sessions) surface. If you want to see which of those a session came from, turn on the **Source** column: it is hidden by default. Click **Add column** at the right edge of the Sessions table header and pick **Source** under **Hidden columns**.

Connected Agent Versions are also readable programmatically — see the [API Reference](/api-reference) for the response shape.

## Troubleshooting

<AccordionGroup>
  <Accordion title="Find agents is disabled, or says to enter the key first">
    Discovery is gated on all three typed values — **Anthropic API key**, **AWS Region**, and **Workspace ID**. Fill in whichever is missing.

    Discovery never runs against a stored key. The form only ever searches with a key you typed into it in this sitting, because a saved key is write-only and has no representation the form could read back.
  </Accordion>

  <Accordion title="Find agents returns nothing">
    The card and the form both say *"No agents or environments found in this workspace."* The usual cause is the wrong **Workspace ID** — check it against `platform.claude.com/settings/workspaces` and confirm the key belongs to that same workspace.
  </Accordion>

  <Accordion title="The connection saved but reads Unreachable">
    The check that runs on save couldn't open a session with the values as stored. The result carries the reason:

    1. An invalid or expired **Anthropic API key** — the saved key is never shown back, so if in doubt, assume it and connect again with a fresh one.
    2. An **AWS Region** that isn't where the workspace runs.
    3. An **Agent** and **Environment** that don't belong together — an agent paired with an environment it isn't published to won't resolve.

    A saved connection can't be corrected in place. Connect the agent again with the right value, confirm the new card reads **Connected**, then **Remove** the one that didn't work.
  </Accordion>

  <Accordion title="The agent doesn't appear in the evaluation launcher">
    The launcher lists connected Agent Versions. Confirm the connection saved — it should be a card under **Project Settings → Connections** — and that you're launching from the same Project the connection belongs to. Asking the Trust Agent *"Which agent is connected now?"* is the quickest cross-check.
  </Accordion>

  <Accordion title="Sync now doesn't import my agent's past conversations">
    That's expected — historical session import isn't available for Claude Managed Agent connections, and picking one under **Sync now** says so rather than importing nothing silently. See [Where sessions come from](#where-sessions-come-from) above.
  </Accordion>
</AccordionGroup>

## Related reading

- **[Projects](/concepts/projects)** — the workspace that owns the connection
- **[Agent Versions](/concepts/agent-versions)** — how a connected agent is tracked and pinned
- **[Agent Profile](/concepts/agent-profile)** — what a connected build exposes
- **[Sessions](/concepts/sessions)** — what conversations against the agent become
- **[Work with the Trust Agent](/how-to/work-with-the-trust-agent)** — driving the assistant that opens the connection card
- **[Connect an AWS Bedrock AgentCore agent](/how-to/connect-agentcore)** — the neighboring connector, with historical session sync
- **[Connect a Salesforce Agentforce agent](/how-to/connect-agentforce)** — the other platform that connects in the chat
- **[Connect a Conversational HTTP agent](/how-to/connect-http-agent)** — the fourth platform in the picker, for an agent on no named vendor platform
